  - platform: qbittorrent
    url: !secret qbittorent_host
    username: !secret qbittorrent_username
    password: !secret qbittorrent_password

  # time_date: configure via UI - Settings -> Integrations -> Time & Date

  # tautulli: configure via UI - Settings -> Integrations -> Tautulli
  # - platform: tautulli
  #   api_key: !secret tautulli_aki_key
  #   host: !secret tautulli_host
  #   port: 8181

  # radarr: configure via UI - Settings -> Integrations -> Radarr
  # - platform: radarr
  #   api_key: !secret radarr_api_key
  #   host: 192.168.1.9

  # systemmonitor: configure via UI - Settings -> Integrations -> System Monitor
